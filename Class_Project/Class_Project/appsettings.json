{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "MyConnectionString": "Data source=myDatabase.db" // key-value pair
    // add this file to .gitignore so the connection string isn't posted on GitHub
  }
}
